// Stub only — contracts-m7.md §E8 (root class verbatim, placeholder input only).
// Real implementation is t-search's (features/search/** — out of scope here).
export default function SearchBox() {
  return (
    <div className="search-box">
      <input type="text" placeholder="검색" aria-label="전역 검색" />
    </div>
  );
}
