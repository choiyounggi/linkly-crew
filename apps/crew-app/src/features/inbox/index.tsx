// Stub only — contracts-m7.md §E8 (root class verbatim, placeholder text).
// Real implementation is t-inbox's (features/inbox/** — out of scope here).
export default function Inbox() {
  return (
    <section className="inbox-view" aria-label="승인함">
      <p>승인함 준비 중</p>
    </section>
  );
}
