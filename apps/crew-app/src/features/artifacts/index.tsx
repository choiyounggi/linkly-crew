// Stub only — contracts-m7.md §E8 (root class verbatim, placeholder text).
// Real implementation is t-artifacts's (features/artifacts/** — out of scope here).
export default function Artifacts() {
  return (
    <section className="artifacts-view" aria-label="아티팩트">
      <p>아티팩트 준비 중</p>
    </section>
  );
}
